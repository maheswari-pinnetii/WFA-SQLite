meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/v1/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@thestackly.com",
    "password": "StacklyWFA2026!"
  }
}

script:post-response {
  if (res.body && res.body.data && res.body.data.token) {
    bru.setVar("bearerToken", res.body.data.token);
  }
}
